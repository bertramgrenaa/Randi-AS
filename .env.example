# Optional: enables live Claude Vision analysis in /api/analyze-door.
# Without this key set, the app falls back to a deterministic mock AI-analysis
# simulator so the full demo flow still works end to end.
ANTHROPIC_API_KEY=
